namespace Truesight.Api
{
    public static class Truesight
    {
        // IAppDomain Domain { get; }

        // includes all members of IAppDomain that use the Domain reference to be invoked
        // the latter also includes all extension methods applicable to IAppDomain
        // hint: generate this part in a separate (partial) file
    }
}